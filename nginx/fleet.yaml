defaultNamespace: nginx
# below diff section added to check the CLI issue: https://github.com/rancher/fleet/issues/4534
diff:
  comparePatches:
  - apiVersion: apps/v1
    kind: Deployment
    name: nginx-keep-3
    namespace: nginx
    operations:
    - op: remove
      path: /spec/replicas
  - apiVersion: v1
    kind: ConfigMap
    name: simple-config
    namespace: nginx
    operations:
    - op: remove
      path: /data/test
