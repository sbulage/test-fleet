apiVersion: v1
kind: ConfigMap
metadata:
  name: fleet-json-bug-test
  namespace: default
data:
  # The text block must render raw flow-style mapping with completely unquoted keys
  config.json: |
    {
      app_name: "flarbor-service",
      debug_enabled: true,
      settings: {
        timeout: 30
      }
    }
