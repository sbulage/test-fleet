apiVersion: v1
kind: ConfigMap
metadata:
  name: fleet-json-bug-test
  namespace: default
data:
  config.json: |
    {
      env: "validation",
      version: "0.15.1-test",
      settings: {
        debug: true,
        retries: 3
      }
    }
