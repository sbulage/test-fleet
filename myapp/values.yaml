  # Values are for dependency nginx
  values:
    nginx:
      service:
        type: ClusterIP
      pdb:
        enabled: false
      networkPolicy:
        enabled: false
      ingress:
        enabled: false
