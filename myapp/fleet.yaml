namespace: fleet-deps-simple

helm:
  releaseName: guestbook-deps-light
  chart: ""
  repo: ""
  version: ""
  # Values are for dependency nginx
  values:
    nginx:
      service:
        type: ClusterIP
      pdb:
        enabled: false
      networkPolicy:
        enabled: false
      ingress:
        enabled: false
  disableDependencyUpdate: false
